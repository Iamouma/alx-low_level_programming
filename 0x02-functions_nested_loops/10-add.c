#include "main.h"
/**
 * add - it adds two integers and returns the result.
 * @k: the first number to be added
 * @l: the second number to be added
 * Return: the sum of two integers.
 */
int add(int k, int l)
{
	return (k + l);
}
